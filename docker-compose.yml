services:
  telegram-nextcloud-bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./backups:/app/backups
    extra_hosts:
      - "host.docker.internal:host-gateway"
